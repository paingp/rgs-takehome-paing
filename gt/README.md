# Reviewed ground truth

One JSON per page, e.g. `page005.json`. Bootstrapped by
`python -m takeoff.vector_gt --page 5 --out gt/page005.json`, then human-reviewed in the UI.
Proposals are never graded against until reviewed.
